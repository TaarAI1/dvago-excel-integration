# Railway monorepo config.
# In the Railway dashboard each service must set its own Root Directory:
#   API service      → Root Directory: /   (repo root)   Dockerfile: Dockerfile
#   Frontend service → Root Directory: /frontend          Dockerfile: Dockerfile

[build]
  builder = "DOCKERFILE"
  dockerfilePath = "Dockerfile"
